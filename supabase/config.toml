project_id = "osuyokvyhiyvyhjrbcxm"

[functions.ai-router]
verify_jwt = false

[functions.process-knowledge]
verify_jwt = false

# Scheduled by pg_cron and also triggered manually from the admin UI.
# Auth is enforced inside the function (SYNC_SECRET header or a signed-in user).
[functions.sync-knowledge-sources]
verify_jwt = false

[functions.outlook-support-agent]
verify_jwt = false

# Public webhook — Green API POSTs incoming WhatsApp messages here (no JWT).
[functions.whatsapp-webhook]
verify_jwt = false

# Zoho OAuth callback — the browser (and Zoho's redirect) hit this with no JWT.
[functions.zoho-oauth-callback]
verify_jwt = false